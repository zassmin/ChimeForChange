class TwitterMessagesController < ApplicationController
  include ApplicationHelper
  
  # def fetch_tweets
  #   @client = client
  # end
  # try to access @client (otherwise make it a constant)
  # save the messages we receive (try to get location)
  # get it to make calls every few min (to keep the feed consistent, cron job?)
end